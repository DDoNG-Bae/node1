doctype html
html
  head
    meta(charset='utf-8')
  body
    h1
      a(href='/board') Server Side JavaScript
    ul
      each topic in topics
        li
          a(href='/board)= topic.title
    artcle
      if topic
        h2= topic.title
        = topic.content
        div= 'by '+topic.name
      else
        h2 Welcome
        | This is server side javascript tutorial.